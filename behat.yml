default:
  autoload:
    '': %paths.base%/test/Behat/bootstrap/
  suites:
    workflowDescriptors:
      paths: [%paths.base%/test/Behat/features/workflow-descriptor]
      contexts:
        - WorkflowDescriptorContext
    workflowEngine:
      paths: [%paths.base%/test/Behat/features/workflow-engine]
      contexts:
        - WorkflowDescriptorContext
  extensions:
    VIPSoft\CodeCoverageExtension\Extension:
      drivers: [local]
      filter:
        whitelist:
          addUncoveredFilesFromWhitelist: true
          include:
            directories:
              'src':
                suffix: .php
      report:
        format:   clover
        options:
          target: %paths.base%/.reports/clover.xml
log-coverage-html:
  extensions:
    VIPSoft\CodeCoverageExtension\Extension:
      report:
        format:   html
        options:
          target: %paths.base%/.reports/coverage-report-html

